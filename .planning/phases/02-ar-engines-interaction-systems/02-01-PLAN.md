---
phase: 02-ar-engines-interaction-systems
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/AR/FullAR/FullARPlacement.cs
  - Assets/_Project/AR/LiteAR/LiteARPlacement.cs
autonomous: true
requirements:
  - AR-01
  - AR-02
  - LITE-01

must_haves:
  truths:
    - "FullARPlacement detects horizontal upward planes using ARRaycastManager."
    - "LiteARPlacement handles simple fixed height spawning without continuous plane recalculations."
  artifacts:
    - path: "Assets/_Project/AR/FullAR/FullARPlacement.cs"
      provides: "ARCore raycasting and object anchoring"
      contains: "ARRaycastManager"
    - path: "Assets/_Project/AR/LiteAR/LiteARPlacement.cs"
      provides: "Simplified ground alignment spawning"
      contains: "LiteARPlacement"
---

<objective>
Implement placement scripts for Full AR (ARCore raycast plane intersection) and Lite AR (fixed base plane mapping) to enable stable placement of 3D food prefabs.

Purpose: Set up the spatial placing mechanism for the two AR-capable rendering tiers.
Output: FullARPlacement.cs and LiteARPlacement.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-foundation-core-detection/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FullARPlacement manager</name>
  <files>Assets/_Project/AR/FullAR/FullARPlacement.cs</files>
  <action>Create FullARPlacement.cs. It must:
  1. Use ARRaycastManager and ARPlaneManager.
  2. Detect screen touch, run raycast filtering for TrackableType.PlaneWithinPolygon (horizontal upward).
  3. When hit occurs, place the active food prefab at hit position/rotation, anchor it (ARAnchor), and trigger a short haptic bump (Handheld.Vibrate).
  4. Ensure only one food item exists (destroy or move the existing object if spawned again).</action>
  <verify>Verify raycasting, vibration, and anchoring calls exist</verify>
  <acceptance_criteria>
    - Assets/_Project/AR/FullAR/FullARPlacement.cs contains "ARRaycastManager"
    - Contains "Handheld.Vibrate()"
    - Employs ARAnchor attachments
  </acceptance_criteria>
  <done>FullARPlacement is coded to anchor assets on ARCore planes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LiteARPlacement manager</name>
  <files>Assets/_Project/AR/LiteAR/LiteARPlacement.cs</files>
  <action>Create LiteARPlacement.cs. It must:
  1. Bypass continuous plane tracking updates.
  2. Spawn the food model relative to a fixed virtual horizontal ground height (e.g., at camera height minus 1.2 meters, or straight onto a reference transform base).
  3. Respond to tap actions to place and anchor the model, but with minimal CPU raycasting footprint.</action>
  <verify>Verify spawning uses static transform reference height and avoids plane raycasts</verify>
  <acceptance_criteria>
    - Assets/_Project/AR/LiteAR/LiteARPlacement.cs contains "MonoBehaviour"
    - Instantiates objects at a defined ground position or base height
  </acceptance_criteria>
  <done>LiteARPlacement is created to support simple, fast placement on mid-tier hardware.</done>
</task>

</tasks>

<verification>
- [ ] Scripts compile without issues.
- [ ] Single active item rule is enforced on placement.
</verification>

<success_criteria>
- Raycasts target only horizontal upward surfaces in Full AR.
- Spawned items are anchored to prevent shifting.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ar-engines-interaction-systems/02-01-SUMMARY.md`
</output>
