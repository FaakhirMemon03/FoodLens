---
phase: 02-ar-engines-interaction-systems
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - Assets/_Project/AR/InteractionHandler.cs
autonomous: true
requirements:
  - GEST-01
  - GEST-02
  - GEST-03
  - GEST-04

must_haves:
  truths:
    - "InteractionHandler reads Touch inputs to rotate (one-finger drag Y-axis) and scale (pinch between 0.8x and 1.2x)."
    - "InteractionHandler implements repositioning via long-press drag along the active plane, and removal on double-tap confirmation."
  artifacts:
    - path: "Assets/_Project/AR/InteractionHandler.cs"
      provides: "Touch gesture input systems"
      contains: "InteractionHandler"
---

<objective>
Implement the gesture control handler (`InteractionHandler.cs`) for Full and Lite AR modes. The gestures must be smooth, low-latency, and support rotation, scale clamping, repositioning on the plane, and deletion.

Purpose: Provide responsive touch interaction controls for visual exploration.
Output: InteractionHandler.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ar-engines-interaction-systems/02-01-PLAN.md
@.planning/phases/02-ar-engines-interaction-systems/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement InteractionHandler touch gesture logic</name>
  <files>Assets/_Project/AR/InteractionHandler.cs</files>
  <action>Create InteractionHandler.cs. It must:
  1. Detect touch events.
  2. Implement single-finger swipe Y-rotation: rotate the selected object transform based on deltaPosition.x.
  3. Implement pinch-to-scale: compute distance delta of 2 touches, update localScale, and clamp scale factors strictly between 0.8x and 1.2x of its default scale.
  4. Implement long-press drag to reposition: if a touch is held on the object for >0.5s, switch to repositioning. Project a raycast from touch position to horizontal planes, and move the object position to the raycast hit point.
  5. Implement double-tap removal: track double-taps on the object. If detected, open a confirmation overlay (or delegate to UI manager) to delete/remove the object.</action>
  <verify>Verify touch checking, gesture thresholds, scaling limits, and raycasting coordinates logic</verify>
  <acceptance_criteria>
    - Assets/_Project/AR/InteractionHandler.cs contains "Input.touchCount"
    - Clamps scale multiplier between 0.8f and 1.2f
    - Contains "Raycast" or plane translation updates
    - Double-tap detection records time between taps
  </acceptance_criteria>
  <done>InteractionHandler is successfully implemented.</done>
</task>

</tasks>

<verification>
- [ ] Code compiles without issues.
- [ ] Gesture limits and drag-to-reposition coordinate changes match requirements.
</verification>

<success_criteria>
- Scaling clamps values strictly between 0.8x and 1.2x.
- Double-taps trigger deletion checks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ar-engines-interaction-systems/02-03-SUMMARY.md`
</output>
