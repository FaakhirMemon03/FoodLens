---
phase: 02-ar-engines-interaction-systems
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - Assets/_Project/AR/FullAR/LightingEstimation.cs
  - Assets/_Project/AR/FullAR/ShadowCatcherController.cs
autonomous: true
requirements:
  - AR-03
  - AR-04
  - LITE-02
  - LITE-03

must_haves:
  truths:
    - "LightingEstimation registers to ARCameraManager.frameReceived to receive lighting updates."
    - "ShadowCatcherController positions a transparent plane with a shadow receiver shader beneath placed food items."
  artifacts:
    - path: "Assets/_Project/AR/FullAR/LightingEstimation.cs"
      provides: "Light estimation tracking and light adjustment"
      contains: "ARCameraManager.frameReceived"
    - path: "Assets/_Project/AR/FullAR/ShadowCatcherController.cs"
      provides: "Transparent shadow caster base manager"
      contains: "ShadowCatcherController"
---

<objective>
Implement Lighting Estimation and Shadow Catcher C# scripts to achieve realistic, grounded food rendering that matches real-world environmental light.

Purpose: Adjust rendering parameters dynamically to blend virtual food into physical rooms.
Output: LightingEstimation.cs and ShadowCatcherController.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ar-engines-interaction-systems/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LightingEstimation component</name>
  <files>Assets/_Project/AR/FullAR/LightingEstimation.cs</files>
  <action>Create LightingEstimation.cs. It must:
  1. Have public references to Light (directional) and ARCameraManager.
  2. Subscribe to ARCameraManager.frameReceived on Enable, and unsubscribe on Disable.
  3. In frameReceived callback, extract ARCameraFrameEventArgs.lightEstimation values:
     - averageBrightness (apply to directional light intensity).
     - averageColorTemperature (apply to directional light color).
     - ambientSphericalHarmonics (apply to RenderSettings.ambientProbe if present).</action>
  <verify>Verify subscription to frameReceived and updates to light settings</verify>
  <acceptance_criteria>
    - Assets/_Project/AR/FullAR/LightingEstimation.cs contains "frameReceived"
    - Modifies target light intensity and color temperature
  </acceptance_criteria>
  <done>LightingEstimation component is fully written.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ShadowCatcherController component</name>
  <files>Assets/_Project/AR/FullAR/ShadowCatcherController.cs</files>
  <action>Create ShadowCatcherController.cs. It must:
  1. Maintain a reference to a shadow catcher plane mesh/transform.
  2. On placement of a food object, align the shadow catcher plane's position/rotation with the detected plane floor height directly underneath the food.
  3. Ensure it renders with a transparent shadow-receiver shader (e.g. URP/Decal or a custom shadow catcher lit material) to hide the plane itself while retaining cast shadows.</action>
  <verify>Verify plane height alignment and positioning logic in relation to food placement</verify>
  <acceptance_criteria>
    - Assets/_Project/AR/FullAR/ShadowCatcherController.cs contains "MonoBehaviour"
    - Sets shadow plane height based on active object position
  </acceptance_criteria>
  <done>ShadowCatcherController is implemented to align shadows with placed objects.</done>
</task>

</tasks>

<verification>
- [ ] Code compiles cleanly.
- [ ] Frame event subscription is correctly released on disable.
</verification>

<success_criteria>
- Lighting estimation updates the main scene directional light parameters.
- Shadow catcher plane maps position dynamically.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ar-engines-interaction-systems/02-02-SUMMARY.md`
</output>
