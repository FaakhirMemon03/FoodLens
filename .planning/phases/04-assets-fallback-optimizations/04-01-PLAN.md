---
phase: 04-assets-fallback-optimizations
plan: "04-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Assets/LODUtility.cs
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
  - ASSET-03

must_haves:
  truths:
    - "LODUtility script calculates mesh triangle counts to verify that LOD0, LOD1, and LOD2 tiers remain under poly limits."
  artifacts:
    - path: "Assets/_Project/Assets/LODUtility.cs"
      provides: "Assets polycount and validation tool"
      contains: "LODUtility"
---

<objective>
Implement an LOD validator utility (`LODUtility.cs`) to inspect 3D assets at runtime/editor, ensuring that meshes load with appropriate poly levels (LOD0: <15k, LOD1: <7k, LOD2: <3k) and use optimized textures.

Purpose: Guarantee 3D meshes conform strictly to performance guidelines to preserve battery and maintain 60 FPS.
Output: LODUtility.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LODUtility validator tool</name>
  <files>Assets/_Project/Assets/LODUtility.cs</files>
  <action>Create LODUtility.cs. It must:
  1. Have a function ValidateAssetLOD(GameObject prefab) to read LODGroup components.
  2. Parse the Renderers in each LOD level.
  3. Traverse child MeshFilters, calculate total triangles using MeshFilter.sharedMesh.triangles.Length / 3.
  4. Compare levels against limits: LOD0 (15000), LOD1 (7000), LOD2 (3000).
  5. Print clear Warning logs indicating if any LOD level exceeds limits, and Success logs if all pass.</action>
  <verify>Verify mesh parser loops and limit checks in LODUtility code</verify>
  <acceptance_criteria>
    - Assets/_Project/Assets/LODUtility.cs contains "LODGroup"
    - Contains sharedMesh triangle parsing calculations
    - Outlines warning comparisons for 15000, 7000, and 3000 tris
  </acceptance_criteria>
  <done>LODUtility script is implemented.</done>
</task>

</tasks>

<verification>
- [ ] Script compiles successfully.
- [ ] Mesh triangle calculations are accurate (triangles index / 3).
</verification>

<success_criteria>
- The validator successfully reads LODGroup levels.
- Warnings are raised for assets violating poly limits.
</success_criteria>

<output>
After completion, create `.planning/phases/04-assets-fallback-optimizations/04-01-SUMMARY.md`
</output>
