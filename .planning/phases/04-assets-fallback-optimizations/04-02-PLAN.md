---
phase: 04-assets-fallback-optimizations
plan: "04-02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - Assets/_Project/Core/Managers/PerformanceMonitor.cs
autonomous: true
requirements:
  - SYS-04

must_haves:
  truths:
    - "PerformanceMonitor measures current frame rate at runtime."
    - "PerformanceMonitor triggers scene routing to LiteAR if FPS remains below 30 for 3 consecutive seconds in FullAR mode."
  artifacts:
    - path: "Assets/_Project/Core/Managers/PerformanceMonitor.cs"
      provides: "Frame-rate tracking and visual downgrade supervisor"
      contains: "PerformanceMonitor"
---

<objective>
Implement the Performance Monitor component (`PerformanceMonitor.cs`) that monitors active frame rates and triggers a dynamic fallback to Lite AR mode if performance dips below 30 FPS for 3 consecutive seconds, protecting device thermal levels.

Purpose: Guarantee smooth user visualization experiences via automatic rendering downgrades.
Output: PerformanceMonitor.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-assets-fallback-optimizations/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PerformanceMonitor C# script</name>
  <files>Assets/_Project/Core/Managers/PerformanceMonitor.cs</files>
  <action>Create PerformanceMonitor.cs. It must:
  1. Calculate running FPS (1 / Time.deltaTime smoothed over 10 frames).
  2. If ModeManager.Instance.CurrentMode is FullAR, check if FPS < 30.
  3. Maintain a duration timer. If FPS is continuously below 30 for 3.0 seconds, trigger dynamic downgrade.
  4. Perform the downgrade: load the "LiteAR" scene using SceneManager, preserving object placement states (by passing position/rotation coordinates or setting a persistence cache in ModeManager).</action>
  <verify>Verify FPS averaging calculations, 3.0s counter logic, and SceneManager loading calls exist</verify>
  <acceptance_criteria>
    - Assets/_Project/Core/Managers/PerformanceMonitor.cs contains "Time.deltaTime"
    - Contains <= 30f frame-rate thresholds
    - Runs a 3.0s continuous trigger timer
    - Triggers SceneManager.LoadScene("LiteAR")
  </acceptance_criteria>
  <done>PerformanceMonitor script is implemented and active.</done>
</task>

</tasks>

<verification>
- [ ] Code compiles without issues.
- [ ] FPS smoothing logic filters short frame spikes.
</verification>

<success_criteria>
- The monitor transitions scenes upon sustained low FPS.
- Object state details are cached to restore position on fallback scenes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-assets-fallback-optimizations/04-02-SUMMARY.md`
</output>
