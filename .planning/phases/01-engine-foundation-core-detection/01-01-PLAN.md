---
phase: 01-engine-foundation-core-detection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Core/ModeDetection/ARMode.cs
  - Assets/_Project/Core/ModeDetection/DeviceThresholds.cs
  - Assets/_Project/Core/ModeDetection/ModeDetectionSystem.cs
autonomous: true
requirements:
  - SYS-01
  - SYS-02
  - SYS-03

must_haves:
  truths:
    - "ARMode enum is defined with FullAR, LiteAR, and FakeAR values."
    - "ModeDetectionSystem uses SystemInfo to query system capabilities and assigns the correct ARMode."
  artifacts:
    - path: "Assets/_Project/Core/ModeDetection/ARMode.cs"
      provides: "ARMode enum"
      contains: "public enum ARMode"
    - path: "Assets/_Project/Core/ModeDetection/DeviceThresholds.cs"
      provides: "Device thresholds configuration"
      contains: "public class DeviceThresholds"
    - path: "Assets/_Project/Core/ModeDetection/ModeDetectionSystem.cs"
      provides: "Core capability scoring logic"
      contains: "class ModeDetectionSystem"
---

<objective>
Set up the core folder architecture of the Unity project and implement the Mode Detection System that analyzes the device's hardware capacity (ARCore support, RAM, GPU memory, and CPU core count) to determine the appropriate AR mode.

Purpose: To establish the hardware scoring logic that routes high-end, mid-range, and low-end devices to Full AR, Lite AR, and Fake AR respectively.
Output: C# scripts defining the ARMode, DeviceThresholds parameters, and the ModeDetectionSystem script.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ARMode enum</name>
  <files>Assets/_Project/Core/ModeDetection/ARMode.cs</files>
  <action>Create a clean ARMode enum script containing FullAR, LiteAR, and FakeAR options under the workspace folder.</action>
  <verify>Verify file contains 'public enum ARMode { FullAR, LiteAR, FakeAR }'</verify>
  <acceptance_criteria>
    - Assets/_Project/Core/ModeDetection/ARMode.cs contains "public enum ARMode"
    - ARMode enum contains FullAR, LiteAR, and FakeAR
  </acceptance_criteria>
  <done>ARMode enum exists with the three required modes.</done>
</task>

<task type="auto">
  <name>Task 2: Create DeviceThresholds config class</name>
  <files>Assets/_Project/Core/ModeDetection/DeviceThresholds.cs</files>
  <action>Create a serializable DeviceThresholds class to define minimum requirements for RAM (Full: 5000MB, Lite: 3000MB) and GPU memory (Full: 1500MB, Lite: 800MB).</action>
  <verify>Verify file exists and is marked [System.Serializable]</verify>
  <acceptance_criteria>
    - Assets/_Project/Core/ModeDetection/DeviceThresholds.cs contains "[System.Serializable]"
    - Contains fields: minRAM_Full, minRAM_Lite, minGPU_Full, minGPU_Lite
  </acceptance_criteria>
  <done>DeviceThresholds class is created with the specified hardware constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ModeDetectionSystem logic</name>
  <files>Assets/_Project/Core/ModeDetection/ModeDetectionSystem.cs</files>
  <action>Implement ModeDetectionSystem inheriting from MonoBehaviour. It must:
  1. Have a static ARMode CurrentMode property.
  2. Implement an IEnumerator DetectMode() coroutine that yields ARSession.CheckAvailability().
  3. If ARSession is Unsupported, fallback immediately to ARMode.FakeAR.
  4. If ARSession is supported, evaluate RAM (SystemInfo.systemMemorySize), GPU memory (SystemInfo.graphicsMemorySize), and CPU Cores (SystemInfo.processorCount) using a scoring system:
     - RAM: >=5000MB (+2 score), >=3000MB (+1 score).
     - GPU Memory: >=1500MB (+2 score), >=800MB (+1 score).
     - CPU Cores: >=8 (+2 score), >=4 (+1 score).
     - Score >= 5 -> FullAR, Score >= 3 -> LiteAR, else -> FakeAR.</action>
  <verify>Verify file contains DecideMode and EvaluateDevice functions matching the logic requirements</verify>
  <acceptance_criteria>
    - Assets/_Project/Core/ModeDetection/ModeDetectionSystem.cs contains "CheckAvailability()"
    - Contains "SystemInfo.systemMemorySize" and "SystemInfo.graphicsMemorySize"
    - Contains "DecideMode" routing logic based on capabilities score
  </acceptance_criteria>
  <done>ModeDetectionSystem decides and sets CurrentMode statically.</done>
</task>

</tasks>

<verification>
- [ ] Scripts compile without syntax errors.
- [ ] Device scoring logic aligns with requirements.
</verification>

<success_criteria>
- Mode detection logic handles both ARCore availability check and device capacity checks.
- All files are created in the correct directories.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-foundation-core-detection/01-01-SUMMARY.md`
</output>
