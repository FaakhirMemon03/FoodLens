---
phase: 01-engine-foundation-core-detection
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Assets/_Project/Core/Managers/BootLoader.cs
  - Assets/_Project/Core/Managers/ModeManager.cs
autonomous: true
requirements:
  - SYS-03

must_haves:
  truths:
    - "BootLoader triggers SceneManager.LoadScene to route users dynamically to the appropriate mode scene."
    - "ModeManager is persistent across scene changes (DontDestroyOnLoad)."
  artifacts:
    - path: "Assets/_Project/Core/Managers/BootLoader.cs"
      provides: "Boot loader scene router"
      contains: "SceneManager.LoadScene"
    - path: "Assets/_Project/Core/Managers/ModeManager.cs"
      provides: "Global persistent manager"
      contains: "DontDestroyOnLoad"
---

<objective>
Implement the Boot Loader component and the global Mode Manager. The Boot Loader will act as the scene director at launch, loading the correct scene for the hardware tier.

Purpose: Automate the routing of users to the correct scene environment (FullAR, LiteAR, or FakeAR) after detection finishes.
Output: BootLoader.cs and ModeManager.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-foundation-core-detection/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ModeManager singleton</name>
  <files>Assets/_Project/Core/Managers/ModeManager.cs</files>
  <action>Create a persistent ModeManager singleton class. It must use DontDestroyOnLoad(gameObject) in Awake. It should expose the detected ARMode from ModeDetectionSystem as a public property so it can be queried globally.</action>
  <verify>Verify file contains Instance singleton reference and DontDestroyOnLoad</verify>
  <acceptance_criteria>
    - Assets/_Project/Core/Managers/ModeManager.cs contains "public static ModeManager Instance"
    - Contains "DontDestroyOnLoad(gameObject)"
  </acceptance_criteria>
  <done>ModeManager singleton is set up.</done>
</task>

<task type="auto">
  <name>Task 2: Implement BootLoader router</name>
  <files>Assets/_Project/Core/Managers/BootLoader.cs</files>
  <action>Create a BootLoader script. In Start(), wait for ModeDetectionSystem.CurrentMode to be finalized (or call it directly if synchronous), and load the scene corresponding to the ARMode enum value:
  - FullAR -> load scene "FullAR"
  - LiteAR -> load scene "LiteAR"
  - FakeAR -> load scene "FakeAR"</action>
  <verify>Verify SceneManager.LoadScene calls are mapped to FullAR, LiteAR, and FakeAR scenes</verify>
  <acceptance_criteria>
    - Assets/_Project/Core/Managers/BootLoader.cs contains "using UnityEngine.SceneManagement;"
    - Contains "SceneManager.LoadScene"
    - Routes according to ModeDetectionSystem.CurrentMode
  </acceptance_criteria>
  <done>BootLoader script is created and ready for scene transitions.</done>
</task>

</tasks>

<verification>
- [ ] Scripts compile without errors.
- [ ] Mode routing switches cover all three enum cases.
</verification>

<success_criteria>
- The scene loading targets the correct scene names ("FullAR", "LiteAR", "FakeAR").
- Singletons and routing scripts are established.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-foundation-core-detection/01-02-SUMMARY.md`
</output>
