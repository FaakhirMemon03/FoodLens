---
phase: 01-engine-foundation-core-detection
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - Assets/_Project/AR/FakeAR/WebCamBackground.cs
  - Assets/_Project/AR/FakeAR/FakeARController.cs
autonomous: true
requirements:
  - FAKE-01
  - FAKE-02
  - FAKE-03

must_haves:
  truths:
    - "WebCamBackground uses WebCamTexture to display the device's live camera output as a background layer."
    - "FakeARController places the 3D food item at a fixed depth from the camera and translates user touches to rotate, scale, or shift the object."
  artifacts:
    - path: "Assets/_Project/AR/FakeAR/WebCamBackground.cs"
      provides: "Camera background feed capture"
      contains: "WebCamTexture"
    - path: "Assets/_Project/AR/FakeAR/FakeARController.cs"
      provides: "Fake AR object manipulation controller"
      contains: "Translate"
---

<objective>
Implement the Fake AR Mode rendering system, which enables low-end devices without ARCore support to display food in an AR-like experience by combining a live camera feed background with a manually controllable 3D food overlay.

Purpose: Provide a fallback AR experience on low-end hardware.
Output: WebCamBackground.cs and FakeARController.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-foundation-core-detection/01-01-PLAN.md
@.planning/phases/01-engine-foundation-core-detection/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WebCamBackground script</name>
  <files>Assets/_Project/AR/FakeAR/WebCamBackground.cs</files>
  <action>Create WebCamBackground.cs. It must:
  1. Instantiate a new WebCamTexture.
  2. Retrieve a Renderer (or RawImage component) and assign the WebCamTexture to its material's mainTexture (or texture property).
  3. Call WebCamTexture.Play() to start camera streaming.
  4. Correctly rotate or scale the display container to match mobile device screen aspect ratios and orientation.</action>
  <verify>Verify script contains WebCamTexture instantiation and Play() call</verify>
  <acceptance_criteria>
    - Assets/_Project/AR/FakeAR/WebCamBackground.cs contains "WebCamTexture"
    - Contains ".Play()"
  </acceptance_criteria>
  <done>WebCamBackground script is implemented and handles camera feed rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FakeARController object manipulator</name>
  <files>Assets/_Project/AR/FakeAR/FakeARController.cs</files>
  <action>Create FakeARController.cs. It must:
  1. Position the active 3D food object transform at a fixed distance (e.g., 1.5 meters) in front of the main camera viewport on Start.
  2. Handle 1-finger drag touch input to translate the object's X/Y position relative to the screen.
  3. Support double-finger pinch touch input to scale the object transform (clamped to 0.8x to 1.2x of its default scale).
  4. Render a fake blob shadow (e.g., using a flat textured plane positioned just below the food item mesh) to create visual depth contact.</action>
  <verify>Verify translation, pinch scaling, and touch logic exist in Update loop</verify>
  <acceptance_criteria>
    - Assets/_Project/AR/FakeAR/FakeARController.cs contains "Input.touchCount"
    - Clamps scaling value between 0.8f and 1.2f
    - Translates screen coordinates to local/world space changes
  </acceptance_criteria>
  <done>FakeARController is programmed to support camera-fixed 3D movement and manual gestures.</done>
</task>

</tasks>

<verification>
- [ ] Scripts compile successfully without warnings.
- [ ] Input handling and clamping boundaries are robust.
</verification>

<success_criteria>
- The camera background is loaded dynamically at runtime.
- Touch input translates to food model transformation in Fake AR.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-foundation-core-detection/01-03-SUMMARY.md`
</output>
