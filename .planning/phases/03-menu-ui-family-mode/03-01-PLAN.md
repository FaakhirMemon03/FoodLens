---
phase: 03-menu-ui-family-mode
plan: "03-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/UI/MenuCarousel.cs
  - Assets/_Project/UI/DetailPanelController.cs
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "MenuCarousel exposes horizontal swipeable carousel selection for food items."
    - "DetailPanelController transitions detail cards onto the screen and sets item metadata."
  artifacts:
    - path: "Assets/_Project/UI/MenuCarousel.cs"
      provides: "UI selection carousel slider"
      contains: "MenuCarousel"
    - path: "Assets/_Project/UI/DetailPanelController.cs"
      provides: "UI details cards panel slides controller"
      contains: "DetailPanelController"
---

<objective>
Build the restaurant-ready interactive menu interface containing a horizontal swipeable item carousel and sliding description panels displaying food name, price, and ordering paths.

Purpose: Let users browse available dishes and select which one to visualize in AR.
Output: MenuCarousel.cs and DetailPanelController.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MenuCarousel selector</name>
  <files>Assets/_Project/UI/MenuCarousel.cs</files>
  <action>Create MenuCarousel.cs. It must:
  1. Define a list of menu data objects (e.g. food names, price, prefabs).
  2. Detect swipe delta or click-next/prev actions to center the active menu item.
  3. Center-lock item positions smoothly using Lerp in the UI ScrollRect layout.
  4. Expose a public delegate or Action Event OnItemChanged to notify placement components of the selected index.</action>
  <verify>Verify swipe centerview lerping and event action registers exist</verify>
  <acceptance_criteria>
    - Assets/_Project/UI/MenuCarousel.cs contains "ScrollRect"
    - Exposes "OnItemChanged" Action or UnityEvent
    - Centers items via Lerping
  </acceptance_criteria>
  <done>MenuCarousel is implemented to support horizontal browsing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DetailPanelController card slide</name>
  <files>Assets/_Project/UI/DetailPanelController.cs</files>
  <action>Create DetailPanelController.cs. It must:
  1. Expose public Text (or TMP_Text) references for Title, Description, and Price fields.
  2. Implement an ShowPanel(string title, string desc, string price) function.
  3. Slide the UI card panel up (using UI RectTransform position Lerp) when an item is selected.
  4. Expose a button callback for 'Add to Cart' / 'Order' actions.</action>
  <verify>Verify text assignments, panel positioning Lerps, and AddToCart click events</verify>
  <acceptance_criteria>
    - Assets/_Project/UI/DetailPanelController.cs contains "TMP_Text" or "Text"
    - Contains Lerping code to animate RectTransform positions
    - Exposes a button listener setup for ordering
  </acceptance_criteria>
  <done>DetailPanelController card slide animator is successfully written.</done>
</task>

</tasks>

<verification>
- [ ] Scripts compile without warnings.
- [ ] UI lerps animate smoothly without frame rate spikes.
</verification>

<success_criteria>
- Carousel selections raise change event notifications.
- Description cards slide into position showing matching prices.
</success_criteria>

<output>
After completion, create `.planning/phases/03-menu-ui-family-mode/03-01-SUMMARY.md`
</output>
