---
phase: 03-menu-ui-family-mode
plan: "03-02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - Assets/_Project/UI/LanguageLocalizer.cs
  - Assets/_Project/UI/FamilyModeManager.cs
autonomous: true
requirements:
  - SYS-05
  - UI-04

must_haves:
  truths:
    - "LanguageLocalizer translates all system strings from English to Urdu when Urdu mode is toggled."
    - "FamilyModeManager scales button layouts (e.g. 1.3x size) when Family Mode is activated to improve finger tap target areas."
  artifacts:
    - path: "Assets/_Project/UI/LanguageLocalizer.cs"
      provides: "Bilingual string translation dictionary"
      contains: "LanguageLocalizer"
    - path: "Assets/_Project/UI/FamilyModeManager.cs"
      provides: "Layout and scale manager for Family Mode UI"
      contains: "FamilyModeManager"
---

<objective>
Implement Language Localization (English/Urdu dictionary translator) and Family Mode Layout Sizer (scaling buttons for older/family users) to fulfill accessibility requirements.

Purpose: Provide an easy-to-use localized accessibility mode.
Output: LanguageLocalizer.cs and FamilyModeManager.cs.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-menu-ui-family-mode/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LanguageLocalizer script</name>
  <files>Assets/_Project/UI/LanguageLocalizer.cs</files>
  <action>Create LanguageLocalizer.cs. It must:
  1. Maintain English-to-Urdu string mapping dictionary for key UI items (e.g. "Add to Cart" -> "کارٹ میں شامل کریں", "Scan Table" -> "میز اسکین کریں").
  2. Implement an Action event OnLanguageChanged that localizable UI components subscribe to.
  3. Expose SetUrduLanguage(bool active) to switch state, trigger translations, and update active labels.</action>
  <verify>Verify localization mappings and event dispatcher exist in code</verify>
  <acceptance_criteria>
    - Assets/_Project/UI/LanguageLocalizer.cs contains "OnLanguageChanged"
    - Defines key Urdu translation strings
    - Contains SetUrduLanguage method
  </acceptance_criteria>
  <done>LanguageLocalizer dictionary and state machine are complete.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FamilyModeManager button sizer</name>
  <files>Assets/_Project/UI/FamilyModeManager.cs</files>
  <action>Create FamilyModeManager.cs. It must:
  1. Track a collection of Button (or RectTransform) elements.
  2. Expose a public SetFamilyMode(bool active) option.
  3. When enabled, scale interactive button RectTransforms by a scale multiplier (e.g. 1.35x width/height) and update font sizes.
  4. When disabled, revert sizes and fonts to baseline dimensions.</action>
  <verify>Verify button resizing loops and font resizing logic are present</verify>
  <acceptance_criteria>
    - Assets/_Project/UI/FamilyModeManager.cs contains "SetFamilyMode"
    - Resizes target RectTransform sizes using multipliers
  </acceptance_criteria>
  <done>FamilyModeManager accessibility scaling layout logic is programmed.</done>
</task>

</tasks>

<verification>
- [ ] Mapped UI translation strings match Urdu terms.
- [ ] Button sizing adjusts margins and scales target objects.
</verification>

<success_criteria>
- Localization updates text elements dynamically.
- Interactive targets scale on toggling Family Mode.
</success_criteria>

<output>
After completion, create `.planning/phases/03-menu-ui-family-mode/03-02-SUMMARY.md`
</output>
